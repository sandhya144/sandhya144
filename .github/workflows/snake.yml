name: Generate Snake

on:
  schedule:
    - cron: "0 */6 * * *"   # runs every 6 hours
  workflow_dispatch: {}
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: sandhya144
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


# name: Generate Snake

# on:
#   schedule:
#     - cron: "0 */6 * * *"   # runs every 6 hours
#   workflow_dispatch: {}
#   push:
#     branches:
#       - main

# jobs:
#   generate:
#     permissions:
#       contents: write
#     runs-on: ubuntu-latest
#     steps:
#       - uses: Platane/snk@v3
#         id: snake-gif
#         with:
#           github_user_name: sandhya144
#           outputs: |
#             dist/github-contribution-grid-snake.svg
#             dist/github-contribution-grid-snake-dark.svg?palette=github-dark

#       # Change empty contribution cells from white to light grey
#       - name: Change empty cells to grey
#         run: |
#           sed -i 's/#ebedf0/#d9d9d9/g' dist/github-contribution-grid-snake.svg
#           sed -i 's/#ffffff/#d9d9d9/g' dist/github-contribution-grid-snake.svg

#       - uses: crazy-max/ghaction-github-pages@v4
#         with:
#           target_branch: output
#           build_dir: dist
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
